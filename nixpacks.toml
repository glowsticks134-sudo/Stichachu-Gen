[phases.setup]
nixPkgs = ["nodejs_22", "nodePackages_latest.pnpm", "python3", "gcc", "gnumake"]

[phases.install]
cmds = [
  "pnpm install --no-frozen-lockfile",
]

[phases.build]
cmds = []

[start]
cmd = "node start.js"
